**/node_modules
**/dist
**/.turbo
**/.vite
**/.next
var
.git
.github
.vscode
.idea
.DS_Store
.env
.env.local
.env.*.local
*.log
pnpm-debug.log*
# apps/api, apps/worker, apps/desktop are NOT excluded here because the web
# Dockerfile needs their package.json files for pnpm workspace resolution.
# Their source is not used at runtime but is small and harmless in the build
# stage; the final nginx image only contains apps/web/dist.
